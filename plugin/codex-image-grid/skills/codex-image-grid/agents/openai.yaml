interface:
  display_name: "Codex Image Grid"
  short_description: "Native macOS image generation and Prompt Batch"
  default_prompt: "Use $codex-image-grid to generate this image set in the native app."

policy:
  allow_implicit_invocation: true
